[Unit]
Description=Lokigi Celery AI Worker
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/path/to/backend
ExecStart=/path/to/venv/bin/celery -A backend.celery_app:celery worker -Q ai -n ai@%h
Restart=always

[Install]
WantedBy=multi-user.target
