  e2e:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          npm install -g playwright
          npm install
          npx playwright install --with-deps

      - name: Start frontend (Next.js)
        run: |
          npm run build
          npm run start &
        env:
          BACKEND_URL: http://localhost:8000

      - name: Wait for frontend
        run: |
          for i in {1..20}; do
            if curl -s http://localhost:3000; then exit 0; fi
            sleep 2
          done
          exit 1

      - name: Run Worker Terminal E2E test
        run: npx playwright test frontend/src/app/dashboard/worker_terminal.e2e.spec.ts
# .github/workflows/qa_global_validator.yml

name: QA Global Validator

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest requests

      - name: Install Node.js dependencies
        run: |
          npm install -g playwright lighthouse
          npx playwright install --with-deps

      - name: Start backend (FastAPI)
        run: |
          pip install -r backend/requirements.txt
          uvicorn backend.main:app --host 0.0.0.0 --port 8000 &
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      - name: Wait for backend
        run: |
          for i in {1..20}; do
            if curl -s http://localhost:8000/docs; then exit 0; fi
            sleep 2
          done
          exit 1

      - name: Run all QA and API tests
        run: |
          pytest tests/
          pytest tests/test_response_bot_qa.py

      - name: Show QA Automation README
        run: cat tests/QA_AUTOMATION_README.md
