# ====================
# LOKIGI - Variables de Entorno
# ====================

# OpenAI (Opcional - para análisis con GPT-4)
OPENAI_API_KEY=sk-proj-tu_api_key_aqui

# Stripe (REQUERIDO para pagos)
# Obtén tus keys en: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_51xxx...  # Usa sk_test_ para pruebas, sk_live_ para producción
STRIPE_WEBHOOK_SECRET=whsec_xxx...  # Obtenerlo al crear webhook en Stripe Dashboard

# Stripe - Configuración del Webhook:
# 1. Ve a: https://dashboard.stripe.com/webhooks
# 2. Crea endpoint: https://tu-dominio.com/api/stripe/webhook
# 3. Selecciona eventos: checkout.session.completed, payment_intent.succeeded
# 4. Copia el "Signing secret" aquí arriba

# Base de Datos
# SQLite para desarrollo (default)
DATABASE_URL=sqlite:///./lokigi.db
# PostgreSQL para producción (descomenta y configura):
# DATABASE_URL=postgresql://usuario:password@localhost:5432/lokigi

# Frontend URL (para redirecciones de Stripe)
FRONTEND_URL=http://localhost:3000

# Email (Opcional - para notificaciones)
# SENDGRID_API_KEY=SG.xxx...
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tu_email@gmail.com
# SMTP_PASSWORD=tu_password

# Slack (Opcional - para notificaciones al equipo)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx/yyy/zzz
